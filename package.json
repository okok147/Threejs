{
  "name": "threejs",
  "version": "0.0.0",
  "private": true,
  "description": "A simple three.js landing page with GitHub Pages deployment",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lab:validate": "node scripts/validate-lab.mjs",
    "lab:check": "npm test && npm run lab:validate && npm run build",
    "test": "node --test"
  },
  "dependencies": {
    "three": "^0.183.2"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  }
}
